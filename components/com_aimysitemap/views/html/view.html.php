<?php
/*
 * Copyright (c) 2017-2025 Aimy Extensions, Netzum Sorglos Software GmbH
 * Copyright (c) 2014-2017 Aimy Extensions, Lingua-Systems Software GmbH
 *
 * https://www.aimy-extensions.com/
 *
 * License: GNU GPLv2, see LICENSE.txt within distribution and/or
 *          https://www.aimy-extensions.com/software-license.html
 */
 defined( '_JEXEC' ) or die(); require_once( JPATH_ADMINISTRATOR . '/components/com_aimysitemap/helpers/language.php' ); require_once( JPATH_ADMINISTRATOR . '/components/com_aimysitemap/helpers/compat.php' ); use Joomla\CMS\MVC\View\HtmlView; use Joomla\CMS\Factory; use Joomla\CMS\Language\Text; use Joomla\CMS\Uri\Uri; class AimySitemapViewHtml extends HtmlView { protected $items = null; protected $data = null; protected $params = null; protected $merge = false; protected $filterlang = false; protected $exclude_img = false; protected $exclude_doc = false; protected $exclude_mm = false; protected $show_credits = true; protected $emojis = false; protected $variant = 'list'; protected $container_style = 'bs2'; static private $container_style_ok = array( 'bs2', 'bs3' ); static private $variants_ok = array( 'list', 'index' ); static private $emojis_ok = array( ); static private $locale = false; public function __construct( $options = null ) { $jin = Factory::getApplication()->input; $v = $jin->get( 'variant', 'list', 'word' ); if ( in_array( $v, self::$variants_ok ) ) { $this->variant = $v; } $cc = $jin->get( 'container_style', 'bs2', 'alnum' ); if ( in_array( $cc, self::$container_style_ok ) ) { $this->container_style = $cc; } $this->merge = $jin->get( 'prevent_duplicate_titles', true, 'bool' ); if ( ! self::$locale ) { $locales = Factory::getLanguage()->getLocale(); if ( is_array( $locales ) && ! empty( $locales ) ) { self::$locale = $locales[ 0 ]; } } parent::__construct( $options ); $this->params = Factory::getApplication()->getParams(); } public function display( $tpl = null ) { $this->items = $this->get( 'Items' ); $errors = $this->get( 'Errors' ); if ( is_array( $errors ) && count( $errors ) ) { throw new Exception( implode( "\n", $errors ), 500 ); return false; } if ( $this->merge ) { $this->_merge_urls_by_title(); } switch ( $this->variant ) { case 'index': $this->data = $this->_build_index(); break; default: $this->data = $this->_build_list(); break; } $this->_prepare_document(); parent::display( $tpl ); } private function _prepare_document() { $app = Factory::getApplication(); $doc = Factory::getDocument(); $menu = $app->getMenu()->getActive(); if ( $menu && $this->params ) { $this->params->def( 'page_heading', $this->params->get( 'page_heading', $menu->title ) ); } $title = ''; if ( $this->params ) { $title = $this->params->get( 'page_title', '' ); } if ( empty( $title ) && ! empty( $menu ) ) { $title = $menu->title; } $title_pos = $app->get( 'sitename_pagetitles', 0 ); switch ( $title_pos ) { case 1: $doc->setTitle( Text::sprintf( 'JPAGETITLE', $app->get( 'sitename', '' ), $title ) ); break; case 2: $doc->setTitle( Text::sprintf( 'JPAGETITLE', $title, $app->get( 'sitename', '' ) ) ); } if ( $desc = AimySitemapCompatHelper::getMenuItemParam( $menu, 'menu-meta_description', false ) ) { $doc->setDescription( $desc ); } else if ( $desc = $app->get( 'MetaDesc', false ) ) { $doc->setDescription( $desc ); } if ( $kw = AimySitemapCompatHelper::getMenuItemParam( $menu, 'menu-meta_keywords', false ) ) { $doc->setMetadata( 'keywords', $kw ); } else if ( $kw = $app->get( 'MetaKeys', false ) ) { $doc->setMetadata( 'keywords', $kw ); } if ( $rob = AimySitemapCompatHelper::getMenuItemParam( $menu, 'robots', false ) ) { $doc->setMetadata( 'robots', $rob ); } else if ( $rob = $app->get( 'robots', false ) ) { $doc->setMetadata( 'robots', $rob ); } else { $doc->setMetadata( 'robots', 'index, follow' ); } } private function _build_list() { usort( $this->items, array( __CLASS__, 'sort_by_title' ) ); return $this->items; } private function _build_index() { $idx = array(); foreach ( $this->items as $item ) { $fc = AimySitemapLanguageHelper::get_index_character( $item->title ); $idx[ $fc ][] = $item; } uksort( $idx, array( __CLASS__, 'sort_by_string_asc' ) ); foreach ( array_keys( $idx ) as $key ) { uasort( $idx[ $key ], array( __CLASS__, 'sort_by_title' ) ); } return $idx; } private function _merge_urls_by_title() { $map = array(); foreach ( $this->items as $item ) { $t = $item->title; if ( ! isset( $map[ $item->title ] ) ) { $map[ $t ] = $item; } else { if ( strlen( $item->url ) < strlen( $map[ $t ]->url ) ) { $map[ $t ] = $item; } } } $this->items = array_values( $map ); } static private function sort_by_title( $a, $b ) { if ( ! is_object( $a ) or ! is_object( $b ) ) { return ( is_object( $a ) ? -255 : 255 ); } return self::sort_by_string_asc( $a->title, $b->title ); } static private function sort_by_string_asc( $a, $b ) { return AimySitemapLanguageHelper::strcasecmp( $a, $b, self::$locale ); } } 
